// Autolinking metadata for React Native (Legacy Architecture).
// Scaffold stub for Chunk 0.1 — declares the native platforms this package
// provides so consumers autolink iOS (podspec) and Android (build.gradle).
// The actual native sources are added in Phases 2 (iOS) and 3 (Android).
module.exports = {
  dependency: {
    platforms: {
      ios: {},
      android: {},
    },
  },
};
