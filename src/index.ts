// react-native-rlottie — public entry point.
//
// Intentionally empty in Chunk 0.1 (repo scaffold). The TypeScript surface is
// implemented in Phase 4:
//   - RlottieView (src/RlottieView.tsx)        — Chunk 4.3
//   - Rlottie module (src/RlottieModule.ts)    — Chunk 4.4
//   - types & source normalization             — Chunks 4.1
// Until then this module exists only so `tsc --noEmit` has an entry point.

export {};
